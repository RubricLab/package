name: Release Package

on:
 push:
  branches:
   - main

jobs:
 release:
  uses: rubriclab/.github/workflow-templates/release-package.yml@main
  secrets:
   GH_PACKAGE_TOKEN: ${{ secrets.GH_PACKAGE_TOKEN }}
